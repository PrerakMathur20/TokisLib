/**
 * Tokis — Performance-first, token-native UI design system
 *
 * This meta-package re-exports everything from /react for convenience.
 * For individual packages, install:
 *   /react   — React components, hooks, and context
 *   /theme   — Precompiled CSS (variables, reset, component styles)
 *   /core    — Headless primitives (state machines, a11y, focus)
 *   /tokens  — Design token definitions (TypeScript + JSON)
 */
'use strict';

module.exports = require('/react');
